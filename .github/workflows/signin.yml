name: Aliyun Signin

on:
  schedule:
   # 每天国际时间 17:20 运行一次, 中国时间 01:20
    - cron: '20 17 * * *'
  workflow_dispatch:
jobs:
  signin:
    name: Aliyun Signin
    runs-on: ubuntu-latest
    steps:
      - uses: ImYrS/aliyun-auto-signin@main
        with:
          REFRESH_TOKENS: ${{ secrets.a3a94799e38e467799a1ac7f9257f9cc }}
          GP_TOKEN: ${{ secrets.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 }}
          PUSH_TYPES: 'pushplus'
          PUSHPLUS_TOKEN: ${{ secrets.5f255852362b4913b5b1255f641f031a }}
